# ApplyPilot search configuration
# Copy to ~/.applypilot/searches.yaml and customize for your job search.
# The wizard (applypilot init) generates a basic version automatically.

# Global include/exclude rules for job titles (case-insensitive, partial matching)
include_titles_with: []
exclude_titles_with:
  - "senior director"
  - "VP "
  - "vice president"
  - "chief"
  - "intern"
  - "internship"
  - "co-op"
  - "clearance required"

# Search queries -- grouped by priority tier.
# Tier 1: High-priority exact matches (searched first, highest precision)
# Tier 2: Strong matches, broader scope
# Tier 3: Wide net, high volume, lower precision
queries:
  - tier: 1
    searches:
      - query: "software engineer"
        # exclude_titles_with: ["frontend"]  # Optional local overrides for this specific query
      - query: "backend engineer"
      - query: "backend developer"
      
  - tier: 2
    searches:
      - query: "full stack developer"
      - query: "full stack engineer"
      - query: "python developer"
      - query: "python engineer"
      - query: "DevOps engineer"
      - query: "site reliability engineer"
      - query: "platform engineer"
      - query: "cloud engineer"
      - query: "data engineer"
      - query: "infrastructure engineer"
      
  - tier: 3
    searches:
      - query: "software developer"
      - query: "developer"
      - query: "systems engineer"
      - query: "automation engineer"
      - query: "solutions engineer"
      - query: "integration engineer"
      - query: "ML engineer"
      - query: "machine learning engineer"
      - query: "AI engineer"
      - query: "security engineer"

# Locations to search
locations:
  - location: "San Francisco, CA"
    remote: false
  - location: "Remote"
    remote: true

# Location filtering -- accept/reject patterns for job location text
location:
  accept_patterns:
    - "San Francisco"
    - "Bay Area"
    - "SF"
    - "California"
    - "CA"
    - "Remote"
    - "Anywhere"
    - "United States"
    - "US"
    - "USA"
  reject_patterns:
    - "New York only"
    - "Seattle only"
    - "onsite only"
    - "London, UK"
    - "India"
    - "Philippines"

# Company filtering -- if accept_patterns is empty, all companies are allowed
# except those matching reject_patterns.
# If accept_patterns has values, only matching companies are kept.
company:
  accept_patterns: []
  reject_patterns:
    - "Example Bad Company"

# Country for Indeed/LinkedIn/Google Jobs searches
country: "USA"

# JobSpy source boards (all available options shown)
boards:
  - indeed
  - linkedin
  - glassdoor
  - zip_recruiter
  - google

# Search defaults
defaults:
  results_per_site: 100  # Max results per board per query
  hours_old: 72          # Only jobs posted within this many hours

